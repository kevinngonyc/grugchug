{
  "name": "@grugchug/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --env-file=../.env --env-file=.env --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir dist",
    "start": "bun --env-file=../../.env --env-file=../.env --env-file=.env dist/index.js",
    "test": "bun test",
    "check:llm": "bun --env-file=../../.env --env-file=../.env --env-file=.env scripts/check-llm.ts",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "@grugchug/shared": "workspace:*",
    "unpdf": "^1.8.1",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
